import React from 'react';
import './heading.css';

const Heading = ({ heads }) => {
    return (
        <div className='heading'>
            .../{heads}...
        </div>
    );
}

export default Heading;
